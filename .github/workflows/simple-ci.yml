name: Simple CI

on:
  workflow_dispatch:

jobs:
  titi:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: action
        uses: ./.github/actions
    
